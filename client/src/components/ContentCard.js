const ContentCard = () => {
    return (
        <div className="content-card">
            <div className="content-task">Enter the task details here</div>
            <button className="content-deleteButton">Delete</button>
        </div>
    );
};

export default ContentCard;